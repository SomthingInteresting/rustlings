// variables2.rs
//
// Execute `rustlings hint variables2` or use the `hint` watch subcommand for a
// hint.

fn main() {
    let x: u64 = 3; // tried adding a type annotation before the value (u64 is 64 bit integer)
    if x == 10 {
        println!("x is ten!");
    } else {
        println!("x is not ten!");
    }
}
